/** 112px (7rem) section gap on lg+ via bottom padding only; no stacked top+bottom on large screens. */
export const landingSectionPad =
  "scroll-mt-[4.25rem] pt-14 pb-14 md:pt-16 md:pb-16 lg:pt-0 lg:pb-28";

/** Hero → first section: 112px below hero on lg+. */
export const landingHeroBottomPad = "lg:pb-28";

/** In-section block before a major anchor (e.g. How it works inside Product). */
export const landingInSectionGap = "mt-20 lg:mt-28";
